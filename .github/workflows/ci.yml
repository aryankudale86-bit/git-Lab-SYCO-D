name: CI

on:
  push:
    branches: [ "feature-1" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run a script
        run: echo "Workflow working!"
